.mypage-wrapper
  = render "header"
  - current_user.students.each do |student|
    = link_to student_reports_path(student) do
      = student.name
      = student.school.name
      = student.grade.name
      = student.subject.name
  = render "footer"